#include <stdio.h>
int main()
 {
  int i,weekdays;

  printf("Enter your no: ");
  scanf("%d",&i);
  (weekdays>= 7) ? 
 printf("there will be no day") : printf("there will be someday");

  return 0;
}